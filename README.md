# TimkaMe — Public Documentation Mirror

Automated mirror of selected documentation files from the private TimkaMe repo.

Purpose: provides Claude web chat with current project context across sessions.

Do not edit files here directly. Changes will be overwritten by the sync workflow.

Source: private repo (auto-synced via GitHub Actions)
Last sync: see LAST-SYNC.md

If stale or broken, trigger manual sync: Actions tab → Sync docs to public mirror → Run workflow (in private repo).
